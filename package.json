{
  "name": "thor-portfolio",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:assets": "node scripts/prepare-assets.mjs",
    "dev": "npm run prepare:assets && vite --host 127.0.0.1 --port 4173",
    "build": "npm run prepare:assets && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "check": "node --check src/main.js && node --check src/world.js && node --check src/storm-audio.js && node --check scripts/prepare-assets.mjs"
  },
  "dependencies": {
    "gsap": "3.15.0",
    "lenis": "1.3.25",
    "three": "0.185.1"
  },
  "devDependencies": {
    "vite": "8.1.5"
  },
  "engines": {
    "node": ">=22.12"
  }
}
